# Only include prost-input and prost-output directories in release archives
# Exclude everything else by default
* export-ignore

# Explicitly include these directories (unset export-ignore)
prost-input/ -export-ignore
prost-output/ -export-ignore